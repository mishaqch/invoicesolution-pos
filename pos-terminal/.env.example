VITE_API_URL=http://localhost:8000

# Phase 0 stubs — real Branch + Terminal models land in Phase 1.
VITE_BRANCH_NAME=Defence Branch
VITE_TERMINAL_NAME=Counter 1

# Dev-only: where to keep the local SQLite file. Production uses the
# Electron userData directory.
POS_DB_PATH=./pos.sqlite
